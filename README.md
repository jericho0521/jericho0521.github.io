
  # Project Website for NeuroRhythm+

  This is a code bundle for Project Website for NeuroRhythm+. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  